// singly linked list algo problem -> SLL.head && nodes && pointers
// each node object has 2 attributes : data, next which comes from the Node Class
// runner is "node" that has a starting point which is at the SLL.head and ends when it points null


